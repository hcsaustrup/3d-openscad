use <components/everything.scad>

// Chieftec BT02 Front Panel Insert
// By Hans Christian Saustrup <hc@saustrup.net>
model_version = "v0.1.0";

// $fn = $preview ? 0 : 25;
$fn = $preview ? 0 : 50;

everything();
